# pnpm Configuration
# Auto-generated by Feature 003: Project Structure Optimization

# Dependency Hoisting Configuration
# Hoist common UI library dependencies to the root node_modules
# to avoid version conflicts and reduce disk space usage
public-hoist-pattern[]=*vue*
public-hoist-pattern[]=*ant-design-vue*
public-hoist-pattern[]=*vxe-table*
public-hoist-pattern[]=*pinia*
public-hoist-pattern[]=*vue-router*
public-hoist-pattern[]=*axios*

# Strict peer dependencies (prevent version mismatches)
strict-peer-dependencies=true

# Auto-install peers (convenience during development)
auto-install-peers=true

# Lockfile settings (ensure reproducible builds)
lockfile=true

# Performance optimizations
prefer-frozen-lockfile=false
