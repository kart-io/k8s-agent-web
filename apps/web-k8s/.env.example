# ==================== 基础配置 ====================

# 开发服务器端口
VITE_PORT=5668

# 应用基础路径
VITE_BASE=/

# 是否打开 Vue Devtools
VITE_DEVTOOLS=false

# 是否注入全局 Loading
VITE_INJECT_APP_LOADING=true

# ==================== Mock 策略配置 ====================
# 详细说明: 查看 docs/MOCK_STRATEGY.md
#
# 架构说明 (统一使用 backend-mock):
# - 所有 Mock 数据都由 backend-mock 服务提供 (http://localhost:5320)
# - 不再使用前端内置 Mock 逻辑
# - 通过环境变量控制哪些 API 使用 Mock，哪些使用真实后端
#
# 三种开发模式:
# 1. 纯前端开发 (推荐) - 所有 API 使用 backend-mock
# 2. 混合模式 - K8s 用 Mock，认证用真实后端
# 3. 完全真实环境 - 所有 API 用真实后端

# --------------------------------------------------
# 模式 1: 纯前端开发 (推荐,无需真实后端)
# --------------------------------------------------
# 1. 启动 Mock 服务器: pnpm -F @vben/backend-mock run start
# 2. 复制下面的配置:

# VITE_GLOB_API_URL=/api
# VITE_NITRO_MOCK=true
# VITE_USE_K8S_MOCK=false
# VITE_USE_CORE_MOCK=false

# 优点:
# - ✅ 所有 API (认证 + K8s) 都走 backend-mock
# - ✅ 支持完整的 CRUD 操作
# - ✅ 无需真实后端服务

# --------------------------------------------------
# 模式 2: 混合模式 (当前配置)
# --------------------------------------------------
# K8s 资源使用 backend-mock，认证服务使用真实后端

VITE_GLOB_API_URL=/api
VITE_NITRO_MOCK=true
VITE_USE_K8S_MOCK=false
VITE_USE_CORE_MOCK=false

# 如需认证使用真实后端，修改为:
# VITE_GLOB_API_URL=http://your-backend:8080/api  # 认证 API 路径
# VITE_USE_CORE_MOCK=false  # 认证使用真实后端
# VITE_USE_K8S_MOCK=false   # K8s 仍使用 backend-mock (通过 VITE_NITRO_MOCK=true)

# --------------------------------------------------
# 模式 3: 完全真实环境 (生产/集成测试)
# --------------------------------------------------
# 所有 API 都使用真实后端

# VITE_GLOB_API_URL=http://your-backend-server:8080/api
# VITE_NITRO_MOCK=false
# VITE_USE_K8S_MOCK=false
# VITE_USE_CORE_MOCK=false

# 前提:
# - 真实后端服务正常运行
# - 后端实现了所有 API 接口

# ==================== API 配置详解 ====================

# VITE_GLOB_API_URL - API 请求基础路径
# - 开发环境通常使用 /api (通过 Vite 代理)
# - 生产环境可以是完整的 URL

# VITE_NITRO_MOCK - 是否启用 Nitro Mock 服务
# - true: Vite 代理 /api 到 http://localhost:5320 (backend-mock)
# - false: 使用 VITE_GLOB_API_URL 配置的地址

# VITE_USE_K8S_MOCK - K8s API Mock 模式
# - true: 使用前端内置 Mock (已废弃，不推荐)
# - false: 路由到 backend-mock 或真实 API (推荐)

# VITE_USE_CORE_MOCK - 核心 API Mock 模式 (认证、用户信息等)
# - true: 使用前端内置 Mock (已废弃，不推荐)
# - false: 路由到 backend-mock 或真实 API (推荐)
# - 未设置: 跟随 VITE_NITRO_MOCK 的设置

# ==================== 开发建议 ====================

# 日常开发:
# - 使用模式 1 (backend-mock)
# - 功能最完整,开发效率最高

# 快速 UI 预览:
# - 使用模式 2 (当前配置)
# - 可以快速查看列表页面效果
# - 但无法测试 CRUD 操作

# 集成测试:
# - 使用模式 3 (真实后端)
# - 验证前后端集成是否正常
